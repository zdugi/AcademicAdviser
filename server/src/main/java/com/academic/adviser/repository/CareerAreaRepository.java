package com.academic.adviser.repository;

import com.academic.adviser.model.CareerArea;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CareerAreaRepository extends JpaRepository<CareerArea, Integer> {
}
